--[[
    Blox Fruits Script Hub — v5 COMBAT FIX
    Author: [K]vk
    Focus: Stand above enemy, proper attack, quest fix
--]]

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local function getGuiParent()
    if gethui then return gethui() end
    local cg = game:GetService("CoreGui")
    if cg then return cg end
    return LocalPlayer:WaitForChild("PlayerGui")
end

local parent = getGuiParent()
for _, v in ipairs(parent:GetChildren()) do
    if v.Name == "BloxHub" then v:Destroy() end
end

-- ══ ISLAND DATA ══
local islands = {
    {name = "Starter Island", minLv = 1, pos = Vector3.new(1076, 16, 1346)},
    {name = "Jungle", minLv = 10, pos = Vector3.new(-1212, 16, 1032)},
    {name = "Pirate Village", minLv = 50, pos = Vector3.new(-1148, 16, -388)},
    {name = "Desert", minLv = 100, pos = Vector3.new(136, 16, -588)},
    {name = "Frozen Village", minLv = 150, pos = Vector3.new(1097, 16, -1290)},
    {name = "Marine Fortress", minLv = 200, pos = Vector3.new(-438, 16, -1800)},
    {name = "Sky Island", minLv = 250, pos = Vector3.new(-4850, 300, -720)},
    {name = "Prison", minLv = 350, pos = Vector3.new(5300, 16, 4760)},
    {name = "Fountain City", minLv = 500, pos = Vector3.new(5133, 16, 4760)},
    {name = "Colosseum", minLv = 600, pos = Vector3.new(-3450, 16, -3700)},
}

-- ══ COLORS ══
local C_BG = Color3.fromRGB(25, 25, 35)
local C_TITLE = Color3.fromRGB(15, 15, 25)
local C_TAB = Color3.fromRGB(35, 35, 50)
local C_TAB_SEL = Color3.fromRGB(55, 85, 210)
local C_ITEM = Color3.fromRGB(30, 30, 45)
local C_TEXT = Color3.fromRGB(230, 230, 245)
local C_TEXT_DIM = Color3.fromRGB(160, 170, 200)
local C_ACCENT = Color3.fromRGB(100, 130, 255)
local C_TOGGLE_OFF = Color3.fromRGB(50, 50, 65)
local C_TOGGLE_ON = Color3.fromRGB(60, 180, 100)

-- ══ GUI ══
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "BloxHub"
ScreenGui.Parent = parent
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.IgnoreGuiInset = true

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 480, 0, 400)
MainFrame.Position = UDim2.new(0.5, -240, 0.5, -200)
MainFrame.BackgroundColor3 = C_BG
MainFrame.BorderSizePixel = 0
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)
local mStroke = Instance.new("UIStroke", MainFrame)
mStroke.Color = C_ACCENT
mStroke.Thickness = 2

local TitleBar = Instance.new("Frame")
TitleBar.Size = UDim2.new(1, 0, 0, 40)
TitleBar.BackgroundColor3 = C_TITLE
TitleBar.BorderSizePixel = 0
TitleBar.Parent = MainFrame
Instance.new("UICorner", TitleBar).CornerRadius = UDim.new(0, 10)
local tFill = Instance.new("Frame")
tFill.Size = UDim2.new(1, 0, 0, 10)
tFill.Position = UDim2.new(0, 0, 1, -10)
tFill.BackgroundColor3 = C_TITLE
tFill.BorderSizePixel = 0
tFill.Parent = TitleBar

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -50, 1, 0)
Title.Position = UDim2.new(0, 14, 0, 0)
Title.BackgroundTransparency = 1
Title.Font = Enum.Font.GothamBold
Title.TextColor3 = C_ACCENT
Title.TextSize = 16
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Text = "BLOX HUB — v5"
Title.Parent = TitleBar

local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 30, 0, 30)
CloseBtn.Position = UDim2.new(1, -36, 0, 5)
CloseBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
CloseBtn.Text = "X"
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.TextSize = 14
CloseBtn.Parent = TitleBar
Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(0, 6)
CloseBtn.MouseButton1Click:Connect(function() ScreenGui:Destroy() end)

-- Drag
local dragging, dragStart, startPos
TitleBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true; dragStart = input.Position; startPos = MainFrame.Position
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = false end
end)
UserInputService.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local d = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + d.X, startPos.Y.Scale, startPos.Y.Offset + d.Y)
    end
end)

-- Tab Bar
local TabBar = Instance.new("Frame")
TabBar.Size = UDim2.new(1, -10, 0, 36)
TabBar.Position = UDim2.new(0, 5, 0, 42)
TabBar.BackgroundTransparency = 1
TabBar.Parent = MainFrame
local TabList = Instance.new("UIListLayout")
TabList.FillDirection = Enum.FillDirection.Horizontal
TabList.HorizontalAlignment = Enum.HorizontalAlignment.Center
TabList.VerticalAlignment = Enum.VerticalAlignment.Center
TabList.Padding = UDim.new(0, 6)
TabList.Parent = TabBar

-- Status Bar
local StatusBar = Instance.new("TextLabel")
StatusBar.Size = UDim2.new(1, -10, 0, 20)
StatusBar.Position = UDim2.new(0, 5, 1, -24)
StatusBar.BackgroundColor3 = C_TITLE
StatusBar.BorderSizePixel = 0
StatusBar.Font = Enum.Font.Gotham
StatusBar.TextColor3 = C_TEXT_DIM
StatusBar.TextSize = 11
StatusBar.TextXAlignment = Enum.TextXAlignment.Left
StatusBar.Text = " San sang..."
StatusBar.Parent = MainFrame
Instance.new("UICorner", StatusBar).CornerRadius = UDim.new(0, 6)

-- Content
local Content = Instance.new("Frame")
Content.Size = UDim2.new(1, -20, 1, -100)
Content.Position = UDim2.new(0, 10, 0, 82)
Content.BackgroundTransparency = 1
Content.Parent = MainFrame

-- ══ TAB SYSTEM ══
local tabs = {}
local function createTab(name)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 145, 0, 30)
    btn.BackgroundColor3 = C_TAB
    btn.Text = name
    btn.Font = Enum.Font.GothamSemibold
    btn.TextColor3 = C_TEXT_DIM
    btn.TextSize = 13
    btn.AutoButtonColor = false
    btn.Parent = TabBar
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)

    local page = Instance.new("Frame")
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.Visible = false
    page.Parent = Content
    local layout = Instance.new("UIListLayout")
    layout.SortOrder = Enum.SortOrder.LayoutOrder
    layout.Padding = UDim.new(0, 6)
    layout.Parent = page
    local pad = Instance.new("UIPadding")
    pad.PaddingTop = UDim.new(0, 2)
    pad.PaddingLeft = UDim.new(0, 2)
    pad.PaddingRight = UDim.new(0, 2)
    pad.Parent = page

    local function selectTab()
        for _, t in ipairs(tabs) do
            t.btn.BackgroundColor3 = C_TAB
            t.btn.TextColor3 = C_TEXT_DIM
            t.page.Visible = false
        end
        btn.BackgroundColor3 = C_TAB_SEL
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        page.Visible = true
    end
    btn.MouseButton1Click:Connect(selectTab)
    table.insert(tabs, {btn = btn, page = page, select = selectTab})
    return page
end

-- ══ UI HELPERS ══
local function createLabel(parent, text, order)
    local l = Instance.new("TextLabel")
    l.Size = UDim2.new(1, 0, 0, 22)
    l.BackgroundTransparency = 1
    l.Font = Enum.Font.GothamBold
    l.TextColor3 = C_ACCENT
    l.TextSize = 12
    l.TextXAlignment = Enum.TextXAlignment.Left
    l.Text = text
    l.LayoutOrder = order or 0
    l.Parent = parent
    return l
end

local function createToggle(parent, text, callback, order)
    local f = Instance.new("Frame")
    f.Size = UDim2.new(1, 0, 0, 40)
    f.BackgroundColor3 = C_ITEM
    f.BorderSizePixel = 0
    f.LayoutOrder = order or 0
    f.Parent = parent
    Instance.new("UICorner", f).CornerRadius = UDim.new(0, 6)

    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(1, -60, 1, 0)
    lbl.Position = UDim2.new(0, 12, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Font = Enum.Font.Gotham
    lbl.TextColor3 = C_TEXT
    lbl.TextSize = 13
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Text = text
    lbl.Parent = f

    local toggleBtn = Instance.new("TextButton")
    toggleBtn.Size = UDim2.new(0, 46, 0, 24)
    toggleBtn.Position = UDim2.new(1, -54, 0.5, -12)
    toggleBtn.BackgroundColor3 = C_TOGGLE_OFF
    toggleBtn.Text = ""
    toggleBtn.AutoButtonColor = false
    toggleBtn.Parent = f
    Instance.new("UICorner", toggleBtn).CornerRadius = UDim.new(1, 0)

    local knob = Instance.new("Frame")
    knob.Size = UDim2.new(0, 18, 0, 18)
    knob.Position = UDim2.new(0, 3, 0.5, -9)
    knob.BackgroundColor3 = Color3.fromRGB(200, 200, 210)
    knob.Parent = toggleBtn
    Instance.new("UICorner", knob).CornerRadius = UDim.new(1, 0)

    local on = false
    toggleBtn.MouseButton1Click:Connect(function()
        on = not on
        if on then
            TweenService:Create(knob, TweenInfo.new(0.15), {Position = UDim2.new(1, -21, 0.5, -9)}):Play()
            toggleBtn.BackgroundColor3 = C_TOGGLE_ON
            knob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        else
            TweenService:Create(knob, TweenInfo.new(0.15), {Position = UDim2.new(0, 3, 0.5, -9)}):Play()
            toggleBtn.BackgroundColor3 = C_TOGGLE_OFF
            knob.BackgroundColor3 = Color3.fromRGB(200, 200, 210)
        end
        if callback then callback(on) end
    end)
    return f
end

local function createSlider(parent, text, min, max, default, callback, order)
    local f = Instance.new("Frame")
    f.Size = UDim2.new(1, 0, 0, 52)
    f.BackgroundColor3 = C_ITEM
    f.BorderSizePixel = 0
    f.LayoutOrder = order or 0
    f.Parent = parent
    Instance.new("UICorner", f).CornerRadius = UDim.new(0, 6)

    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(1, -20, 0, 20)
    lbl.Position = UDim2.new(0, 12, 0, 6)
    lbl.BackgroundTransparency = 1
    lbl.Font = Enum.Font.Gotham
    lbl.TextColor3 = C_TEXT
    lbl.TextSize = 12
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Text = text .. ": " .. tostring(default)
    lbl.Parent = f

    local bar = Instance.new("Frame")
    bar.Size = UDim2.new(1, -24, 0, 8)
    bar.Position = UDim2.new(0, 12, 0, 34)
    bar.BackgroundColor3 = Color3.fromRGB(45, 45, 60)
    bar.BorderSizePixel = 0
    bar.Parent = f
    Instance.new("UICorner", bar).CornerRadius = UDim.new(1, 0)

    local fill = Instance.new("Frame")
    fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
    fill.BackgroundColor3 = C_ACCENT
    fill.BorderSizePixel = 0
    fill.Parent = bar
    Instance.new("UICorner", fill).CornerRadius = UDim.new(1, 0)

    local sld = false
    local function update(x)
        local rel = math.clamp((x - bar.AbsolutePosition.X) / bar.AbsoluteSize.X, 0, 1)
        local val = math.floor(min + (max - min) * rel)
        fill.Size = UDim2.new(rel, 0, 1, 0)
        lbl.Text = text .. ": " .. tostring(val)
        if callback then callback(val) end
    end
    bar.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            sld = true; update(i.Position.X)
        end
    end)
    UserInputService.InputEnded:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then sld = false end
    end)
    UserInputService.InputChanged:Connect(function(i)
        if sld and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then update(i.Position.X) end
    end)
    return f
end

-- ══ BUILD TABS ══
local farmTab = createTab("Auto Farm")
local chestTab = createTab("Auto Chest")
local flyTab = createTab("Fly")

-- ══ FARM TAB ══
createLabel(farmTab, "-- COMBAT --", 1)

local autoFarm = false
local autoQuest = false
local autoIsland = false
local useSkill = false
local levelRange = 20

createToggle(farmTab, "Auto Farm (Tim & danh NPC)", function(s) autoFarm = s end, 2)
createToggle(farmTab, "Auto Quest (Nhan & hoan thanh)", function(s) autoQuest = s end, 3)
createToggle(farmTab, "Auto Chuyen Dao (Theo Level)", function(s) autoIsland = s end, 4)

createLabel(farmTab, "-- SETTINGS --", 5)
createSlider(farmTab, "Level Range (+/-)", 5, 50, 20, function(v) levelRange = v end, 6)
createToggle(farmTab, "Dung Skill thay vi Melee", function(s) useSkill = s end, 7)

-- ══ CHEST TAB ══
createLabel(chestTab, "-- CHEST FARMING --", 1)

local autoChest = false
local chestCount = 0

createToggle(chestTab, "Auto Chest (Quet & thu thap)", function(s)
    autoChest = s
    if s then chestCount = 0 end
end, 2)

local chestLbl = Instance.new("TextLabel")
chestLbl.Size = UDim2.new(1, 0, 0, 24)
chestLbl.BackgroundTransparency = 1
chestLbl.Font = Enum.Font.Gotham
chestLbl.TextColor3 = C_TEXT_DIM
chestLbl.TextSize = 12
chestLbl.TextXAlignment = Enum.TextXAlignment.Left
chestLbl.Text = "Ruong da thu: 0"
chestLbl.LayoutOrder = 3
chestLbl.Parent = chestTab

-- ══ FLY TAB ══
createLabel(flyTab, "-- FLIGHT CONTROL --", 1)
local flyOn = false
local flySpeed = 50

createToggle(flyTab, "Bat Fly (WASD + Space/Ctrl)", function(s) flyOn = s end, 2)
createSlider(flyTab, "Toc do bay", 10, 300, 50, function(v) flySpeed = v end, 3)

createLabel(flyTab, "-- HUONG DAN --", 4)
local guide = Instance.new("TextLabel")
guide.Size = UDim2.new(1, 0, 0, 80)
guide.BackgroundTransparency = 1
guide.Font = Enum.Font.Gotham
guide.TextColor3 = C_TEXT_DIM
guide.TextSize = 12
guide.TextWrapped = true
guide.TextXAlignment = Enum.TextXAlignment.Left
guide.TextYAlignment = Enum.TextYAlignment.Top
guide.Text = "W: Tien | S: Lui\nA: Trai | D: Phai\nSpace: Len | Ctrl: Xuong\nTang toc do neu bi lag"
guide.LayoutOrder = 5
guide.Parent = flyTab

tabs[1].select()

-- ══ UTILITY ══
local function getChar()
    return LocalPlayer.Character
end
local function getRoot()
    local c = getChar()
    return c and c:FindFirstChild("HumanoidRootPart")
end
local function getHum()
    local c = getChar()
    return c and c:FindFirstChildOfClass("Humanoid")
end
local function getLevel()
    local d = LocalPlayer:FindFirstChild("Data")
    if d and d:FindFirstChild("Level") then return d.Level.Value end
    return 1
end

local function getTargetIsland()
    local myLv = getLevel()
    local target = islands[1]
    local idx = 1
    for i, island in ipairs(islands) do
        if myLv >= island.minLv then
            target = island
            idx = i
        end
    end
    return target, idx
end

-- ══ VIRTUAL SERVICES ══
local vUser, vInput
pcall(function() vUser = game:GetService("VirtualUser") end)
pcall(function() vInput = game:GetService("VirtualInputManager") end)

-- ══ ATTACK FUNCTION (FIXED - STAND ABOVE HEAD) ══
local function attackNPC(target)
    if not target then return end
    local hum = target:FindFirstChildOfClass("Humanoid")
    local root = target:FindFirstChild("HumanoidRootPart") or target:FindFirstChild("Torso") or target:FindFirstChild("UpperTorso")
    if not hum or not root or hum.Health <= 0 then return end

    local myRoot = getRoot()
    local myHum = getHum()
    if not myRoot or not myHum or myHum.Health <= 0 then return end

    -- TELEPORT ABOVE ENEMY HEAD (Y + 8 studs)
    -- Enemy can't hit us, we can hit them
    local abovePos = root.Position + Vector3.new(0, 8, 0)
    myRoot.CFrame = CFrame.new(abovePos, root.Position)

    -- Look down at enemy
    myRoot.CFrame = CFrame.new(abovePos, Vector3.new(root.Position.X, abovePos.Y, root.Position.Z))

    -- Click attack (VirtualUser - main attack method in Blox Fruits)
    if vUser then
        pcall(function()
            vUser:CaptureController()
            vUser:Button1Down(Vector2.new())
            task.wait(0.05)
            vUser:Button1Up(Vector2.new())
        end)
    end

    -- Also activate tool if equipped
    local tool = getChar():FindFirstChildOfClass("Tool")
    if tool then
        pcall(function() tool:Activate() end)
    else
        -- Equip first tool from backpack
        local bp = LocalPlayer:FindFirstChild("Backpack")
        if bp then
            for _, item in ipairs(bp:GetChildren()) do
                if item:IsA("Tool") then
                    pcall(function() myHum:EquipTool(item) end)
                    task.wait(0.03)
                    pcall(function() item:Activate() end)
                    break
                end
            end
        end
    end

    -- Skill attacks (Z, X, C, V, F)
    if useSkill and vInput then
        for _, key in ipairs({Enum.KeyCode.Z, Enum.KeyCode.X, Enum.KeyCode.C, Enum.KeyCode.V, Enum.KeyCode.F}) do
            pcall(function()
                vInput:SendKeyEvent(true, key, false, game)
                task.wait(0.03)
                vInput:SendKeyEvent(false, key, false, game)
            end)
        end
    end
end

-- ══ ENEMY DETECTION (FIXED) ══
local function getBestNPC()
    local myLvl = getLevel()
    local root = getRoot()
    if not root then return nil end

    local best, bestScore = nil, math.huge

    -- Blox Fruits stores enemies in multiple possible locations
    local containers = {
        Workspace:FindFirstChild("Enemies"),
        Workspace:FindFirstChild("Enemy"),
        Workspace,
    }

    for _, container in ipairs(containers) do
        if container then
            for _, obj in ipairs(container:GetChildren()) do
                if obj:IsA("Model") then
                    local hum = obj:FindFirstChildOfClass("Humanoid")
                    local r = obj:FindFirstChild("HumanoidRootPart") or obj:FindFirstChild("Torso") or obj:FindFirstChild("UpperTorso")
                    if hum and r and hum.Health > 0 and not Players:GetPlayerFromCharacter(obj) then
                        -- Get enemy level
                        local el = 1
                        local lvlObj = obj:FindFirstChild("Level")
                        if lvlObj then
                            if lvlObj:IsA("IntValue") or lvlObj:IsA("NumberValue") then
                                el = lvlObj.Value
                            elseif lvlObj:IsA("TextLabel") then
                                el = tonumber(lvlObj.Text:match("%d+")) or 1
                            end
                        end
                        -- Also check Humanoid name for level
                        if el == 1 then
                            local nameMatch = obj.Name:match("%[(%d+)%]")
                            if nameMatch then el = tonumber(nameMatch) end
                        end

                        -- Check level range
                        if el <= myLvl + levelRange and el >= myLvl - levelRange - 10 then
                            local d = (root.Position - r.Position).Magnitude
                            -- Prefer higher level enemies that are closer
                            local score = d - (el * 1.5)
                            if score < bestScore then
                                bestScore = score
                                best = obj
                            end
                        end
                    end
                end
            end
        end
    end

    -- Fallback: search wider without level restriction
    if not best then
        for _, container in ipairs(containers) do
            if container then
                for _, obj in ipairs(container:GetChildren()) do
                    if obj:IsA("Model") then
                        local hum = obj:FindFirstChildOfClass("Humanoid")
                        local r = obj:FindFirstChild("HumanoidRootPart") or obj:FindFirstChild("Torso")
                        if hum and r and hum.Health > 0 and not Players:GetPlayerFromCharacter(obj) then
                            local d = (root.Position - r.Position).Magnitude
                            if d < 500 and d < bestScore then
                                bestScore = d
                                best = obj
                            end
                        end
                    end
                end
            end
        end
    end

    return best
end

-- ══ QUEST SYSTEM (COMPLETELY FIXED) ══
local function getQuestGiver()
    local root = getRoot()
    if not root then return nil, nil end

    -- Method 1: Search for NPC models with quest-related names
    for _, obj in ipairs(Workspace:GetDescendants()) do
        if obj:IsA("Model") and not Players:GetPlayerFromCharacter(obj) then
            local name = obj.Name:lower()
            -- Blox Fruits quest NPCs
            if name:match("quest") or name:match("giver") or name:match("npc") or name:match("snow") or name:match("marine") or name:match("pirate") or name:match("bandit") or name:match("monkey") or name:match("saber") then
                local part = obj:FindFirstChild("HumanoidRootPart") or obj:FindFirstChildWhichIsA("Part") or obj:FindFirstChild("Torso")
                if part then
                    local d = (root.Position - part.Position).Magnitude
                    if d < 3000 then
                        return obj, part
                    end
                end
            end
        end
    end

    -- Method 2: Search for ProximityPrompts
    for _, obj in ipairs(Workspace:GetDescendants()) do
        if obj:IsA("ProximityPrompt") then
            local promptName = (obj.ObjectText or ""):lower()
            local action = (obj.ActionText or ""):lower()
            if promptName:match("quest") or promptName:match("nhiem") or action:match("accept") or action:match("start") then
                local parent = obj.Parent
                local part = parent and (parent:FindFirstChildWhichIsA("Part") or parent:FindFirstChild("HumanoidRootPart"))
                if part then
                    local d = (root.Position - part.Position).Magnitude
                    if d < 3000 then
                        return parent, part
                    end
                end
            end
        end
    end

    return nil, nil
end

local function handleQuest()
    local root = getRoot()
    if not root then return end

    local questGiver, questPart = getQuestGiver()
    if not questGiver or not questPart then return end

    local d = (root.Position - questPart.Position).Magnitude

    -- Teleport directly to quest giver
    if d > 8 then
        root.CFrame = CFrame.new(questPart.Position + Vector3.new(0, 3, 0))
        task.wait(0.2)
    end

    -- Method 1: Fire ProximityPrompt
    local prompt = questGiver:FindFirstChildOfClass("ProximityPrompt") or questPart:FindFirstChildOfClass("ProximityPrompt")
    if prompt then
        pcall(function() fireproximityprompt(prompt) end)
        task.wait(0.3)
    end

    -- Method 2: Try Blox Fruits remotes
    local comm = ReplicatedStorage:FindFirstChild("Remotes")
    if comm then
        local communicator = comm:FindFirstChild("Communicator")
        if communicator then
            pcall(function() communicator:InvokeServer("StartQuest") end)
            pcall(function() communicator:InvokeServer("AcceptQuest") end)
            pcall(function() communicator:FireServer("StartQuest") end)
            pcall(function() communicator:FireServer("AcceptQuest") end)
        end
    end

    -- Method 3: Try touching the quest NPC
    if questPart then
        pcall(function()
            firetouchinterest(root, questPart, 0)
            task.wait(0.1)
            firetouchinterest(root, questPart, 1)
        end)
    end
end

-- ══ ISLAND ROTATION ══
local currentIslandIdx = 1
local function checkIslandRotation()
    if not autoIsland then return end

    local myLv = getLevel()
    local target, idx = getTargetIsland()
    local root = getRoot()
    if not root then return end

    if idx > currentIslandIdx then
        StatusBar.Text = " Dang chuyen dao: " .. target.name .. "..."
        currentIslandIdx = idx
        root.CFrame = CFrame.new(target.pos)
        task.wait(1)
        if autoQuest then
            handleQuest()
            task.wait(0.5)
        end
        StatusBar.Text = " Da den: " .. target.name
    end
end

-- ══ CHEST SYSTEM (FIXED) ══
local function getAllChests()
    local chests = {}
    for _, obj in ipairs(Workspace:GetDescendants()) do
        if (obj:IsA("Model") or obj:IsA("Part")) then
            local name = obj.Name:lower()
            if name:match("chest") or name:match("ruong") or name:match("box") or name:match("crate") or name:match("treasure") then
                local part = obj:IsA("Part") and obj or obj:FindFirstChildWhichIsA("Part")
                if part then
                    local root = getRoot()
                    if root then
                        local d = (root.Position - part.Position).Magnitude
                        table.insert(chests, {model = obj, part = part, dist = d})
                    end
                end
            end
        end
    end
    table.sort(chests, function(a, b) return a.dist < b.dist end)
    return chests
end

-- ══ FARM LOOP (FIXED) ══
task.spawn(function()
    while task.wait(0.1) do
        if autoFarm then
            pcall(function()
                local root = getRoot()
                local hum = getHum()
                if not root or not hum or hum.Health <= 0 then return end

                -- Check island rotation
                checkIslandRotation()

                -- Handle quest
                if autoQuest then
                    handleQuest()
                end

                -- Find and attack nearest NPC
                local npc = getBestNPC()
                if npc then
                    local npcName = npc.Name
                    StatusBar.Text = " Dang farm: " .. npcName
                    attackNPC(npc)
                else
                    StatusBar.Text = " Dang tim NPC..."
                    -- Move to island center to find enemies
                    local island = getTargetIsland()
                    root.CFrame = CFrame.new(island.pos + Vector3.new(math.random(-30, 30), 20, math.random(-30, 30)))
                    task.wait(0.3)
                end
            end)
        end
    end
end)

-- ══ CHEST LOOP (FIXED) ══
task.spawn(function()
    while task.wait(0.5) do
        if autoChest then
            pcall(function()
                local root = getRoot()
                if not root then return end

                local chests = getAllChests()
                if #chests > 0 then
                    local chest = chests[1]
                    StatusBar.Text = " Dang mo ruong... (" .. #chests .. " ruong)"

                    -- Teleport directly into chest
                    root.CFrame = CFrame.new(chest.part.Position)
                    task.wait(0.2)

                    -- Fire prompt
                    local prompt = chest.model:FindFirstChildOfClass("ProximityPrompt") or chest.part:FindFirstChildOfClass("ProximityPrompt")
                    if prompt then
                        pcall(function() fireproximityprompt(prompt) end)
                    end

                    -- Touch
                    pcall(function()
                        firetouchinterest(root, chest.part, 0)
                        task.wait(0.1)
                        firetouchinterest(root, chest.part, 1)
                    end)

                    chestCount = chestCount + 1
                    chestLbl.Text = "Ruong da thu: " .. tostring(chestCount)
                    StatusBar.Text = " Da thu ruong #" .. chestCount
                    task.wait(0.5)
                else
                    StatusBar.Text = " Khong con ruong, dang cho..."
                    task.wait(2)
                end
            end)
        end
    end
end)

-- ══ FLY SYSTEM ══
local flyVel, flyGyro
local keys = {W=false, A=false, S=false, D=false, Space=false, Ctrl=false}

UserInputService.InputBegan:Connect(function(input)
    local k = input.KeyCode
    if k == Enum.KeyCode.W then keys.W = true
    elseif k == Enum.KeyCode.A then keys.A = true
    elseif k == Enum.KeyCode.S then keys.S = true
    elseif k == Enum.KeyCode.D then keys.D = true
    elseif k == Enum.KeyCode.Space then keys.Space = true
    elseif k == Enum.KeyCode.LeftControl then keys.Ctrl = true
    end
end)

UserInputService.InputEnded:Connect(function(input)
    local k = input.KeyCode
    if k == Enum.KeyCode.W then keys.W = false
    elseif k == Enum.KeyCode.A then keys.A = false
    elseif k == Enum.KeyCode.S then keys.S = false
    elseif k == Enum.KeyCode.D then keys.D = false
    elseif k == Enum.KeyCode.Space then keys.Space = false
    elseif k == Enum.KeyCode.LeftControl then keys.Ctrl = false
    end
end)

RunService.RenderStepped:Connect(function()
    if not flyOn then
        if flyVel then flyVel:Destroy(); flyVel = nil end
        if flyGyro then flyGyro:Destroy(); flyGyro = nil end
        return
    end
    pcall(function()
        local root = getRoot()
        local hum = getHum()
        if not root or not hum or hum.Health <= 0 then return end
        if not flyVel or not flyVel.Parent then
            flyVel = Instance.new("BodyVelocity")
            flyVel.MaxForce = Vector3.new(1, 1, 1) * math.huge
            flyVel.Velocity = Vector3.zero
            flyVel.Parent = root
        end
        if not flyGyro or not flyGyro.Parent then
            flyGyro = Instance.new("BodyGyro")
            flyGyro.MaxTorque = Vector3.new(1, 1, 1) * math.huge
            flyGyro.P = 10000
            flyGyro.CFrame = root.CFrame
            flyGyro.Parent = root
        end
        local cam = Workspace.CurrentCamera
        local look = cam.CFrame.LookVector
        local right = cam.CFrame.RightVector
        local move = Vector3.zero
        if keys.W then move = move + look end
        if keys.S then move = move - look end
        if keys.A then move = move - right end
        if keys.D then move = move + right end
        if keys.Space then move = move + Vector3.new(0, 1, 0) end
        if keys.Ctrl then move = move + Vector3.new(0, -1, 0) end
        if move.Magnitude > 0 then move = move.Unit * flySpeed end
        flyVel.Velocity = move
        flyGyro.CFrame = cam.CFrame
    end)
end)

LocalPlayer.CharacterAdded:Connect(function()
    if flyVel then flyVel:Destroy(); flyVel = nil end
    if flyGyro then flyGyro:Destroy(); flyGyro = nil end
end)

-- ══ ANTI-AFK ══
LocalPlayer.Idled:Connect(function()
    if vUser then
        pcall(function()
            vUser:CaptureController()
            vUser:ClickButton2(Vector2.new())
            vUser:ClickButton1(Vector2.new())
        end)
    end
end)

-- ══ INIT ══
pcall(function()
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "Blox Hub v5",
        Text = "Loaded! Stand above enemy, fixed quest & chest!",
        Duration = 4
    })
end)

print("[Blox Hub v5] Loaded successfully!")
